<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/card_padding"
    app:cardCornerRadius="@dimen/radius_md"
    app:cardElevation="@dimen/card_elevation_subtle"
    app:cardBackgroundColor="@color/scandi_surface"
    app:strokeColor="@color/scandi_border"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="@dimen/small_gap"
            android:textColor="@color/scandi_text_high"
            android:textSize="@dimen/text_size_subheader"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="@dimen/section_gap"
            android:textColor="@color/scandi_text_muted"
            android:textSize="@dimen/text_size_body" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_close"
            android:layout_width="match_parent"
            android:layout_height="@dimen/btn_min_height"
            android:text="Close"
            style="@style/ButtonSecondary" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
